export default function EquipoPage() {
  return (
    <div>
      <h1 style={{ fontSize: "1.25rem", fontWeight: 700, marginBottom: "0.5rem" }}>Equipo</h1>
      <p style={{ color: "var(--muted)", fontSize: "0.875rem", marginBottom: "2rem" }}>Invitá colaboradores y asigná roles (owner · admin · staff · scanner)</p>
      <div style={{ background: "var(--surface)", border: "1px solid var(--border)", borderRadius: 8, padding: "3rem", textAlign: "center", color: "var(--muted)" }}>
        <p>Próximamente: gestión de equipo por email.</p>
      </div>
    </div>
  );
}
